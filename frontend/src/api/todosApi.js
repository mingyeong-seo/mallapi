import axios from 'axios';
import { API_SERVER_HOST } from './productsApi';

const host = `${API_SERVER_HOST}/todo`; // todos API 전용 prefix

// export const getList = async ~~{
//     const res = axios.get(`${const}`);

//     return res.data;
// }
